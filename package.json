{
  "name": "all-the-package-repos",
  "version": "1.2.0",
  "description": "All the repository URLs in the npm registry as an object whose keys are package names and values are URLs",
  "main": "index.js",
  "repository": "https://github.com/zeke/all-the-package-repos",
  "author": "Zeke Sikelianos <zeke@sikelianos.com>",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js > index.json",
    "release": "scripts/release.sh",
    "test": "mocha && standard"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "ora": "^1.1.0",
    "package-stream": "^2.0.0",
    "standard": "^8.6.0"
  }
}
